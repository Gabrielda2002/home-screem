---
interface Props {
  title: string;
  image: string;
}

const { title, image } = Astro.props;
---

<h2 class="text-xl font-semibold text-gray-800 mb-2">{title}</h2>
{image && <img src={image} alt={title} class="w-40 h-14 p-2" />}
