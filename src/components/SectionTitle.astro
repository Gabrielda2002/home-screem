---
interface Props {
  title: string;
  image?: string;
}

const { title, image } = Astro.props;
---

<h2 class="section-title">{title}</h2>
{image && <img src={image} alt={title} class="eps-logo" />}
