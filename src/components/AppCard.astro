---
interface Props {
  title: string;
  icon: string;
  bgColor: string;
  url: string;
}

const { title, icon, bgColor, url } = Astro.props;
---

<div
  class={`p-2 rounded-xl text-card-foreground shadow-md hover:shadow-lg hover:scale-95 transition-all duration-200 h-auto ${bgColor}`}
>
  <a href={url} class="flex flex-col items-center">
    <span class="text-xl">{icon}</span>
    <span class="text-gray-900 text-sm font-semibold">{title}</span>
  </a>
</div>
